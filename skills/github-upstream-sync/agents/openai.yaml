interface:
  display_name: "Github Upstream Sync"
  short_description: "Create GitHub Actions workflows that sync forks from upstream"
  brand_color: "#24292F"
  default_prompt: "Use $github-upstream-sync to create a fork sync workflow."
policy:
  allow_implicit_invocation: false
